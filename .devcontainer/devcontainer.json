{
  "name": "ROS2 Humble Dev",

  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },

  "workspaceFolder": "/workspaces/lcr",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces,type=bind",

  "remoteUser": "root",

  "runArgs": [
    "--net=host",
    "--gpus=all",
    "-e", "DISPLAY=${localEnv:DISPLAY}",
    "-v", "/tmp/.X11-unix:/tmp/.X11-unix:rw"
  ],

  "postCreateCommand": "bash -c 'apt-get update && source /opt/ros/humble/setup.bash && cd /workspaces/lcr && rosdep update && rosdep install --from-paths src --ignore-src -r -y'"
}